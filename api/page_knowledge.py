"""
页面知识库 —— 15 种页面类型的结构化知识

从前端帮助文档提取，用于注入 AI 助手的 system prompt，
使 AI 能精准理解当前页面的功能、流程和交互方式。

每个页面类型包含:
- purpose: 页面用途（1-2句）
- features: 核心功能列表（3-5条）
- workflows: 常用操作流程（2-3条）
- variables: 特定变量/引用语法（如有）
- tips: 重要提示/最佳实践（如有）
"""

PAGE_KNOWLEDGE: dict[str, dict] = {
    "dashboard": {
        "purpose": "平台全局状态概览页面，提供 KPI 关键指标卡片、执行趋势图和测试覆盖率热力图。",
        "features": [
            "项目过滤：按项目筛选仪表盘数据，切换项目后所有指标实时更新",
            "覆盖率热力图：按模块 × 维度（场景/Mock/数据工厂/巡检）展示各资产的覆盖矩阵",
            "执行趋势图：折线图展示最近的执行通过率趋势（支持 24h/7d/30d）",
            "KPI 卡片：API 总数、场景数、执行通过率、SLA 可用性、活跃巡检、AI 生成任务统计",
        ],
        "workflows": [
            "查看全局概览 → 关注 KPI 异常指标 → 点击卡片下钻到对应页面排查问题",
            "查看执行趋势图 → 发现通过率下降 → 进入执行历史页查看失败记录",
            "检查覆盖率热力图 → 发现覆盖盲区 → 跳转到对应模块补充测试资产",
        ],
    },
    "api": {
        "purpose": "单个 API 的详细信息页，包含请求/响应查看、AI 分析、文档断言生成、发送请求等功能。",
        "features": [
            "请求/响应查看：结构化展示 Headers、Query、Body（支持 JSON/XML/表单）",
            "AI 智能分析：自动分析 API 结构并生成质量评分和改进建议",
            "文档生成：基于 AI 自动生成 API 使用文档（摘要、描述、参数、响应字段）",
            "断言生成与编辑：自动生成多种类型断言（状态码、JSONPath、响应头、响应时间、正则），提供可视化断言编辑器",
            "直接发送请求：内置请求发送器，接口地址与参数自动替换为所选环境的对应值",
            "鉴权配置：支持 None / Bearer Token / Basic Auth / API Key / OAuth 2.0 等鉴权方式",
            "前置/后置脚本：支持自定义 Python/JavaScript 脚本，用于动态构造参数、数据加解密、结果校验",
        ],
        "workflows": [
            "导入 API → 进入详情页 → AI 分析接口结构 → 查看质量评分 → AI 生成文档和断言 → 审核采纳",
            "进入 API 详情 → 配置鉴权信息 → 选择执行环境 → 发送请求 → 查看实时响应 → 从响应中提取变量",
        ],
        "variables": "{{step_N.response.body.path}} 引用上游步骤响应字段；{{env.BASE_URL}} 引用环境变量；{{factory.field}} 引用数据工厂字段；{{sql_step_N.rows[0].column}} 引用数据库查询结果。",
        "tips": [
            "断言内容由 AI 自动生成，建议人工二次确认后再采纳",
            "接口地址中的 host 部分会自动替换为所选执行环境的 Base URL",
            "参数引用支持嵌套语法，如 {\"user_id\": {{step_1.response.body.id}}}",
        ],
    },
    "api_list": {
        "purpose": "API 资产集中管理页面，支持搜索、筛选、批量操作和多种格式导入。",
        "features": [
            "多种导入方式：Swagger/OpenAPI、Postman、HAR、mitmproxy 实时抓包、cURL 粘贴",
            "批量操作：批量删除、批量导出、批量 AI 分析",
            "列表视图：显示 API 方法、路径、标签、项目、质量评分、最后更新时间",
            "筛选搜索：按方法、路径、标签、项目搜索过滤",
            "单接口快速创建：手动填写请求信息新建 API",
        ],
        "workflows": [
            "mitmproxy 实时抓包 / 浏览器导出 HAR → 粘贴到导入 → 系统自动解析创建 API → 进入列表查看",
            "在列表搜索/筛选找到目标 API → 批量选中 → 批量 AI 分析 → 等待分析完成 → 查看质量评分",
        ],
        "tips": [
            "导入时会自动去重，已存在的 API 会进入差异对比而非重复创建",
            "支持按域名过滤控制哪些域名的请求会被收录（设置 → 项目 → 域名过滤）",
        ],
    },
    "scenario": {
        "purpose": "测试场景详情页，支持 DAG 可视化编排多步骤业务流程、配置参数传递和断言，查看执行结果。",
        "features": [
            "DAG 可视化编排：拖拽式步骤编排，可视化展示步骤间的参数传递和依赖关系",
            "步骤编辑：支持 API 调用、Mock 请求、数据工厂引用、断言设置、SQL 查询、脚本步骤等多类型步骤",
            "批量执行：支持场景内步骤并行/串行执行策略，执行前可选执行环境",
            "执行跟踪：实时查看执行进度、步骤级输入输出和断言结果",
            "数据引用：步骤间通过变量引用语法传递响应数据或数据工厂生成的数据",
        ],
        "workflows": [
            "新建场景 → 设置名称/标签 → 在 DAG 编排器中添加步骤（API 调用/断言/延迟）→ 配置步骤间参数传递 → 保存 → 选择环境执行 → 查看实时进度和步骤结果",
            "打开已有场景 → 查看执行历史 → 对比两次执行结果差异 → 发现回归问题 → 修复后重新执行",
        ],
        "variables": "{{step_N.response.body.field}} 引用上游步骤响应；{{step_N.response.headers.Name}} 引用响应头；{{step_N.response.status}} 引用状态码；{{env.VAR}} 引用环境变量；{{factory.template_name.field}} 引用数据模板；{{sql_step_N.rows[0].col}} 引用 SQL 查询结果。",
        "tips": [
            "执行前可在底部选择执行环境",
            "支持拖拽步骤节点调整执行顺序",
            "同一场景在不同环境执行后，可在执行历史中使用「结果对比」逐步骤对比",
        ],
    },
    "scenario_list": {
        "purpose": "测试场景列表与管理页面，支持场景的创建、编辑、执行和结果查看。",
        "features": [
            "场景列表：显示场景名称、项目、标签、步骤数、最后执行状态",
            "快速创建：新建空场景或通过 AI 一键生成完整测试场景",
            "筛选搜索：按名称、标签、状态搜索场景",
            "批量管理：批量执行、批量删除、批量导出场景",
        ],
        "workflows": [
            "浏览场景列表 → 筛选目标场景 → 点击进入详情 → 编辑步骤 → 执行验证",
            "导入 API 后 → 在 API 详情页点击「生成场景」→ AI 自动编排 → 在审核中心采纳 → 场景列表可见",
            "选择已有场景 → 切换目标执行环境 → 分别执行 → 对比各环境结果发现环境差异或回归缺陷",
        ],
        "tips": [
            "建议将回归测试场景加入巡检任务，实现定时自动化回归",
            "执行环境在「设置 → 执行环境」中预先配置 Base URL 和变量",
        ],
    },
    "execution": {
        "purpose": "单次执行详情页，查看请求/响应详情、断言结果、错误信息和 AI 自动诊断结果。",
        "features": [
            "步骤详情：查看每步的请求/响应详情（Headers、Body、状态码、耗时）",
            "断言结果：逐断言展示通过/失败状态，失败断言高亮显示实际值与期望值",
            "AI 诊断：执行失败时自动调用 AI 分析失败原因并给出修复建议",
            "结果对比：对比两次执行的差异，发现回归问题",
        ],
        "workflows": [
            "收到失败告警 → 进入执行详情页 → 查看失败步骤 → 展开断言结果定位失败断言 → 查看 AI 诊断建议 → 修复后重跑",
            "查看某次执行 → 点击「结果对比」→ 选择历史执行 → 逐步骤对比请求/响应差异",
        ],
        "tips": [
            "AI 诊断结果仅供参考，建议结合业务逻辑判断",
            "执行记录保留完整的请求/响应快照，可用于事后审计",
        ],
    },
    "execution_list": {
        "purpose": "查看所有类型的执行历史记录，包括单接口、场景和巡检的执行结果。",
        "features": [
            "执行记录列表：显示执行类型、对象名称、状态、耗时、触发方式、执行时间",
            "筛选过滤：按类型（API/场景/巡检）、状态（成功/失败/运行中）、时间范围筛选",
            "批量操作：批量重跑失败的执行",
        ],
        "workflows": [
            "进入执行历史页 → 按类型/状态筛选 → 找到失败记录 → 点击进入详情 → AI 诊断原因",
            "定期查看执行趋势 → 发现某 API 连续失败 → 进入该 API 详情页检查接口变更或环境问题",
        ],
    },
    "monitor": {
        "purpose": "定时巡检监控管理页面，配置巡检任务、查看告警记录和健康趋势，自动轮询接口健康状态并多渠道告警。",
        "features": [
            "巡检任务管理：定义巡检对象（场景/API）、执行频率（cron 表达式）、超时时间",
            "定时执行：基于 cron 自动触发，支持手动立即执行",
            "告警管理：查看告警记录、告警详情、恢复时间",
            "多渠道通知：钉钉机器人、企业微信、Slack Webhook、自定义 Webhook",
            "健康趋势：按时间段展示接口可用率、响应时间趋势图",
        ],
        "workflows": [
            "确认目标 API/场景已就绪 → 新建巡检任务 → 选择监控对象 → 设置 cron 执行频率 → 配置告警渠道 → 启用任务 → 系统定时执行 → 异常时自动告警",
            "收到告警通知 → 进入巡检页查看告警详情 → 点击关联执行记录 → AI 诊断失败原因 → 修复后观察恢复",
        ],
        "variables": "cron 表达式支持五字段格式：分 时 日 月 周。示例：*/5 * * * *（每5分钟）、0 9 * * *（每天9点）、0 * * * 1-5（工作日每小时）。",
        "tips": [
            "告警渠道需先在设置中配置",
            "支持通过 WebSocket 接收实时告警推送",
            "建议核心接口至少设置每 5-30 分钟一次巡检",
        ],
    },
    "factory": {
        "purpose": "测试数据工厂，管理可复用的测试数据模板和数据集，支持 Faker 方法批量生成多样化测试数据。",
        "features": [
            "数据模板：定义数据结构，每个字段可绑定生成规则（固定值/Faker 方法/正则表达式）",
            "字段编辑器：可视化编辑字段名、类型、生成规则",
            "Faker 方法：内置 50+ Faker 方法（姓名、地址、邮箱、手机号、日期、UUID 等）",
            "数据集管理：批量预览、导出（JSON/CSV）、导入生成的数据",
            "模板引用：场景步骤中通过 {{factory.template_name.field}} 引用模板字段，运行时动态生成数据",
        ],
        "workflows": [
            "新建数据模板 → 定义字段（名称/邮箱/手机/日期等）→ 为每个字段选择 Faker 方法 → 预览生成效果 → 保存 → 在场景步骤中引用模板",
            "为 API 创建数据模板 → 绑定 API ID 关联 → 在场景创建时自动推荐该 API 的数据模板",
        ],
        "variables": "{{factory.template_name.field}} 在场景步骤中引用模板字段；字段支持三种模式：固定值（如 \"active\"）、Faker 方法（如 name.findName()）、正则表达式（如 ORD-[A-Z0-9]{8}）。",
        "tips": [
            "支持生成批量数据（1-1000 条），可直接导出为 JSON/CSV",
            "建议为每个核心 API 创建对应的数据模板，方便场景复用",
        ],
    },
    "generations": {
        "purpose": "AI 生成内容的审核管理中心，集中审核、采纳或放弃 AI 产出的文档、断言、场景、数据模板等内容。",
        "features": [
            "生成列表：按类型（文档/断言/场景/数据模板/聊天建议）查看所有 AI 产出物",
            "审核操作：采纳（替换现有内容）、部分采纳（仅合并选中项）、放弃",
            "批量审核：支持批量通过或批量拒绝 AI 生成结果",
            "差异对比：采纳前可预览新旧内容的差异对比",
        ],
        "workflows": [
            "API 导入 → AI 分析 → AI 生成文档/断言 → 进入审核中心 → 预览生成内容 → 确认无误后采纳 → 内容写入 API DSL",
            "AI Chat 提出修改建议 → 自动创建生成记录 → 在审核中心查看 → 人工审核后采纳/放弃",
            "AI 批量生成多个 API 的断言 → 进入审核中心 → 按 API 筛选 → 逐条审核或批量通过",
        ],
        "tips": [
            "AI 生成内容建议人工二次确认后再采纳，避免错误覆盖已有资产",
            "审核状态为 pending_review 的内容等待处理，采纳后状态变为 applied",
        ],
    },
    "coverage": {
        "purpose": "测试资产覆盖率分析页面，以热力图形式按模块 × 维度展示各个维度对 API 的覆盖情况。",
        "features": [
            "热力图视图：按模块（行）× 维度（列）展示覆盖热力矩阵，颜色深浅表示覆盖比例高低",
            "维度筛选：支持按场景/Mock/数据工厂/巡检等维度单独查看覆盖详情",
            "覆盖率统计：显示各模块、各维度的覆盖率百分比及未覆盖 API 数量",
            "下钻分析：点击热力图单元格，查看未覆盖 API 详细列表并快速跳转创建",
        ],
        "workflows": [
            "进入覆盖度页 → 查看热力图 → 发现某模块在「场景」维度覆盖率为 0 → 点击单元格下钻 → 查看未覆盖 API → 跳转为该 API 创建场景",
            "定期查看覆盖度 → 对比历史覆盖趋势 → 发现覆盖盲区扩大 → 批量补充测试资产",
        ],
        "tips": [
            "五个评估维度：文档(doc)、断言(asserts)、场景(scenario)、监控(monitor)、执行(execute)",
            "模块名按 API 路径首段自动分组，未分类 API 归入 _unclassified 模块",
        ],
    },
    "knowledge": {
        "purpose": "知识库页面，管理接口约定、断言模式、文档模式等可复用知识，支持四层记忆架构（L1-L4）。",
        "features": [
            "L1 长期记忆：HAR 导入时自动提取 API 字段模式，跨项目复用，支持手动确认/编辑",
            "L2 项目记忆：AI 分析产出的文档模板、场景模式、字段 schema，按项目隔离",
            "L3 会话记忆：AI 对话结束后自动归档，ReMe LLM 生成结构化摘要，30天 TTL",
            "L4 对话上下文：AI Chat 多轮对话实时上下文（Redis），24h TTL",
            "语义检索：跨层向量搜索，根据查询语义匹配 L1/L2/L3 中最相关的记忆条目",
        ],
        "workflows": [
            "HAR 导入 API → 系统自动提取字段模式存入 L1 → 进入知识库页查看/确认/编辑 → 后续导入自动复用",
            "使用 AI Chat 完一轮对话 → 对话自动归档到 L3 → 下次对话时可检索到相关历史记忆",
        ],
        "tips": [
            "L1 跨项目共享，适合沉淀通用的字段模式和断言模式",
            "L2 按项目隔离，适合特定项目的业务知识",
            "知识条目支持按类型（field_pattern/assertion_pattern/doc_pattern/scenario_pattern）和标签筛选",
        ],
    },
    "settings": {
        "purpose": "平台全局配置页面，包含 LLM 配置、AI 任务管理、提示词管理、死信队列、执行环境、通用设置、项目管理、告警渠道等。",
        "features": [
            "LLM 配置：配置 AI 模型提供商（OpenAI/DeepSeek/Ollama/LM Studio/自定义）、API Key、模型名称和参数",
            "AI 任务管理：查看 AI 分析、生成、诊断等后台异步任务的状态和进度",
            "提示词管理：自定义 AI Prompt 模板，按任务类型（文档生成/断言生成/场景生成/数据分析/失败诊断）分别配置",
            "执行环境：管理多套执行环境（开发/测试/预发布/生产），配置 Base URL、变量等",
            "项目管理：创建、编辑、删除项目，配置域名黑白名单过滤",
            "告警渠道：管理钉钉/企微/Slack/Webhook 告警通知渠道",
        ],
        "workflows": [
            "首次使用 → 进入设置 → LLM 配置 → 选择提供商并填入 API Key → 测试连接 → 保存 → AI 功能可用",
            "新增执行环境 → 进入设置 → 执行环境 → 新建 → 配置环境名/Base URL/变量 → 保存 → 场景执行时可选择",
        ],
        "tips": [
            "LLM API Key 加密存储，不会在日志或前端明文展示",
            "执行环境的变量支持引用其他变量，如 {{env.BASE_URL}}/api/v1",
            "用户管理仅管理员角色可访问",
        ],
    },
    "import_diffs": {
        "purpose": "HAR/Swagger 等外部格式导入时的差异检测页面，对比新旧接口发现变更并决定是否同步更新。",
        "features": [
            "差异检测：导入时自动对比已有 API，发现路径、方法、请求参数、响应结构等变更",
            "字段级对比：并排展示新旧接口的 Header、Query、Body 逐字段差异，高亮新增/删除/修改",
            "同步决策：对每个差异字段选择「采纳变更」或「保留旧版」，支持全部采纳/全部放弃",
            "变更记录：每次差异比对的对比结果和决策记录均保留，可追溯接口演进历史",
            "批量处理：支持按接口批量预览差异并一键同步",
        ],
        "workflows": [
            "导入 HAR/Swagger 等外部文件 → 系统检测到已存在的 API → 自动进入差异比对 → 查看并排字段级对比 → 逐字段决定采纳或保留 → 提交同步 → API 资产按决策更新",
            "查看差异列表 → 按严重程度(severity)筛选 → 优先处理 critical/high 差异 → 确认变更或驳回",
        ],
        "tips": [
            "差异比对页面按接口分组展示，每个接口独立决策",
            "差异记录可追溯，便于审计接口变更历史",
            "状态包括：pending（待处理）、confirmed（已确认采纳）、auto_fixed（自动修复）、dismissed（已忽略）",
        ],
    },
    "mock_services": {
        "purpose": "独立 Mock 服务管理，模拟第三方或内部依赖服务的响应行为，支持路由规则匹配和动态响应模板。",
        "features": [
            "路由规则：支持路径前缀/精确匹配、HTTP 方法筛选",
            "响应配置：自定义状态码、响应头、响应体（支持模板变量，可引用请求参数动态生成响应）",
            "调用日志：记录每次 Mock 调用的请求信息、匹配规则和响应内容",
            "延迟模拟：可配置响应延迟时间，模拟网络波动",
            "多环境独立：每个 Mock 服务独立 URL，支持多个版本并行运行",
        ],
        "workflows": [
            "进入 Mock 服务页 → 新建 Mock 服务 → 添加路由规则（路径、HTTP 方法）→ 配置响应模板（状态码、Body、Headers）→ 启用服务 → 获取 Mock URL → 在场景或请求中替换真实地址为 Mock URL",
            "第三方服务不可用时 → 创建该服务的 Mock → 配置预期响应 → 场景执行时自动走 Mock → 查看调用日志验证匹配",
        ],
        "variables": "响应体支持模板变量：{{request.params.key}} 引用请求路径参数；{{request.body.field}} 引用请求体字段；{{$randomInt(min,max)}} 生成随机整数；{{$isoTimestamp}} 生成 ISO 时间戳。",
        "tips": [
            "调用日志可帮助调试 Mock 规则是否按预期匹配",
            "Mock 服务 URL 格式：{base}/mock/{service-slug}，slug 创建后不可修改",
        ],
    },
}
